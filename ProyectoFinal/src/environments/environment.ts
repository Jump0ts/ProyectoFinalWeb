// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebase: {
    apiKey: "AIzaSyAuKSTWAo_NvVXEYC2M8Ch-L7dl20bobOE",
    authDomain: "recetario-3c20b.firebaseapp.com",
    databaseURL: "https://recetario-3c20b.firebaseio.com",
    projectId: "recetario-3c20b",
    storageBucket: "recetario-3c20b.appspot.com",
    messagingSenderId: "213216926889",
    appId: "1:213216926889:web:726ecba0b3c8413ffc9db8",
    measurementId: "G-CNJXV39TPP"
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
